import React, { PropsWithChildren } from 'react';
type Props = PropsWithChildren<{
    id?: string;
    additionalClassNames?: string;
}>;
/**
 * Prefixes a given string with `wp-admin-bar-`.
 * This is needed in order for the WordPress core icons (Dashicons) to display.
 */
export declare function wpAdminBar(id?: string): string;
/**
 * The outermost element for a FaustToolbarNode.
 */
export declare function ToolbarNode({ id, children, additionalClassNames, ...props }: Props): React.JSX.Element;
export {};
