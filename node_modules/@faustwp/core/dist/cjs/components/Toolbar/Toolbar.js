"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Toolbar = exports.ToolbarAwaitUser = exports.AuthenticatedToolbar = void 0;
const client_1 = require("@apollo/client");
const react_1 = __importStar(require("react"));
const client_js_1 = require("../../client.js");
const useAuth_js_1 = require("../../hooks/useAuth.js");
const index_js_1 = require("../../wpHooks/index.js");
const Edit_js_1 = require("./nodes/Edit.js");
const GraphiQL_js_1 = require("./nodes/GraphiQL.js");
const MyAccount_js_1 = require("./nodes/MyAccount.js");
const SiteName_js_1 = require("./nodes/SiteName.js");
const ToolbarNode_js_1 = require("./ToolbarNode.js");
/**
 * The component to actually render the toolbar. At this point we can assume
 * there is a proper authenticated user.
 */
function AuthenticatedToolbar({ seedNode }) {
    /**
     * Define Toolbar Nodes that should be included by default.
     */
    const coreToolbarNodes = (0, react_1.useMemo)(() => {
        return [
            {
                id: 'site-name',
                location: 'primary',
                component: react_1.default.createElement(SiteName_js_1.SiteName, null),
            },
            {
                id: 'edit',
                location: 'primary',
                component: react_1.default.createElement(Edit_js_1.Edit, { seedNode: seedNode }),
            },
            {
                id: 'graphiql',
                location: 'primary',
                component: react_1.default.createElement(GraphiQL_js_1.GraphiQL, null),
            },
            {
                id: 'my-account',
                location: 'secondary',
                component: react_1.default.createElement(MyAccount_js_1.MyAccount, null),
                additionalClassNames: 'with-avatar',
            },
        ];
    }, [seedNode]);
    const [toolbarNodes, setToolbarNodes] = (0, react_1.useState)(coreToolbarNodes);
    /**
     * Handle Toolbar nodes.
     */
    (0, react_1.useEffect)(() => {
        const filteredNodes = index_js_1.hooks.applyFilters('toolbarNodes', coreToolbarNodes, {
            seedNode,
        });
        const uniqueIds = new Set(filteredNodes.map((nodes) => nodes.id));
        if (uniqueIds.size < filteredNodes.length) {
            throw new Error('Toolbar Nodes must have unique keys.');
        }
        setToolbarNodes(filteredNodes);
    }, [coreToolbarNodes, seedNode]);
    /**
     * Handle adding `admin-bar` body class on render.
     *
     * This could eventually be handled with a Faust bodyClass hook
     * @link https://developer.wordpress.org/reference/hooks/body_class/
     */
    (0, react_1.useEffect)(() => {
        document === null || document === void 0 ? void 0 : document.body.classList.add('admin-bar');
        // Cleanup body class when this component unmounts.
        // eslint-disable-next-line consistent-return
        return () => {
            document === null || document === void 0 ? void 0 : document.body.classList.remove('admin-bar');
        };
    }, []);
    const primaryNodes = toolbarNodes.filter(({ location }) => location === 'primary');
    const secondaryNodes = toolbarNodes.filter(({ location }) => location === 'secondary');
    return (react_1.default.createElement("div", { id: "wpadminbar", className: "nojq" },
        react_1.default.createElement("div", { id: "wp-toolbar", className: "quicklinks", role: "navigation", "aria-label": "Toolbar" },
            react_1.default.createElement("ul", { id: "wp-admin-bar-root-default", className: "ab-top-menu", "aria-label": "primary toolbar menu items" }, primaryNodes.map((_a) => {
                var { component, id } = _a, props = __rest(_a, ["component", "id"]);
                return (
                // eslint-disable-next-line react/jsx-props-no-spreading
                react_1.default.createElement(ToolbarNode_js_1.ToolbarNode, Object.assign({ key: id, id: id }, props), component));
            })),
            react_1.default.createElement("ul", { id: "wp-admin-bar-top-secondary", className: "ab-top-secondary ab-top-menu", "aria-label": "secondary toolbar menu items" }, secondaryNodes.map((_a) => {
                var { component, id } = _a, props = __rest(_a, ["component", "id"]);
                return (
                // eslint-disable-next-line react/jsx-props-no-spreading
                react_1.default.createElement(ToolbarNode_js_1.ToolbarNode, Object.assign({ key: id, id: id }, props), component));
            })))));
}
exports.AuthenticatedToolbar = AuthenticatedToolbar;
/**
 * With an authenticated user, make a request for the viewer in WPGraphQL and
 * get the user' preference whether to display the toolbar.
 */
function ToolbarAwaitUser({ seedNode }) {
    const client = (0, client_js_1.getApolloAuthClient)();
    const { data, error } = (0, client_1.useQuery)((0, client_1.gql) `
      {
        viewer {
          shouldShowFaustToolbar
        }
      }
    `, { client });
    /**
     * If the above query for "shouldShowFaustToolbar" fails, it likely means
     * that the user doesn't have a version of the FaustWP plugin installed where
     * this field was made available. In that case, don't throw an error, and
     * just continue with showing the toolbar.
     */
    if (error) {
        return react_1.default.createElement(AuthenticatedToolbar, { seedNode: seedNode });
    }
    if (!data) {
        return null;
    }
    if (data.viewer.shouldShowFaustToolbar === false) {
        return null;
    }
    return react_1.default.createElement(AuthenticatedToolbar, { seedNode: seedNode });
}
exports.ToolbarAwaitUser = ToolbarAwaitUser;
/**
 * Renders a Toolbar that is based on WordPress' own toolbar.
 */
function Toolbar({ seedNode }) {
    const { isAuthenticated } = (0, useAuth_js_1.useAuth)();
    if (isAuthenticated !== true) {
        return null;
    }
    return react_1.default.createElement(ToolbarAwaitUser, { seedNode: seedNode });
}
exports.Toolbar = Toolbar;
