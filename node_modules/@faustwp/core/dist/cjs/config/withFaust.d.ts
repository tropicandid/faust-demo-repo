import { NextConfig } from 'next';
import { Redirect, Header } from 'next/dist/lib/load-custom-routes.js';
export interface WithFaustConfig {
    previewDestination?: string;
}
export declare function createRedirects(nextConfig?: NextConfig, redirectFn?: NextConfig['redirects'], previewDestination?: string): Promise<Redirect[]>;
export declare function addHeaders(nextConfig?: NextConfig, existingHeaders?: NextConfig['headers']): Promise<Header[]>;
/**
 * A helper function to merge Faust related Next.js config with a user defined Next.js config.
 *
 * @param {NextConfig} exportedUserNextConfig The existing config to be exported prior to adding Faust
 * @param {WithFaustConfig} withFaustConfig The Configuration for Faust
 * @returns {NextConfig} The modified config to be exported
 */
export declare function withFaust(exportedUserNextConfig?: NextConfig, withFaustConfig?: WithFaustConfig): NextConfig;
