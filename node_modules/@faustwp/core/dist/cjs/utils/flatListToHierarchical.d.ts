export interface Params {
    idKey?: string;
    parentKey?: string;
    childrenKey?: string;
}
type Data = Record<string | number, unknown>;
/**
 * Converts a flat list to hierarchical.
 *
 * @param data The data items as an array.
 * @param param1 The data item property keys.
 * @returns Data Array
 */
export declare function flatListToHierarchical(data?: Data[], { idKey, parentKey, childrenKey, }?: Params): Data[];
export {};
