import React from 'react';
import { useRouter } from 'next/router.js';
import { getAdminUrl } from '../../../lib/getAdminUrl.js';
import { ToolbarItem } from '../index.js';
export function Edit({ seedNode }) {
    var _a, _b;
    const { query: { p, typeName }, } = useRouter();
    if ((seedNode === null || seedNode === void 0 ? void 0 : seedNode.isFrontPage) || (seedNode === null || seedNode === void 0 ? void 0 : seedNode.isPostsPage)) {
        return null;
    }
    // eslint-disable-next-line no-underscore-dangle
    // eslint-disable-next-line no-underscore-dangle
    const postType = (_a = seedNode === null || seedNode === void 0 ? void 0 : seedNode.__typename) !== null && _a !== void 0 ? _a : typeName;
    const postId = (_b = seedNode === null || seedNode === void 0 ? void 0 : seedNode.databaseId) !== null && _b !== void 0 ? _b : p;
    if (!postId || !postType) {
        return null;
    }
    const editPostUrl = getAdminUrl(`post.php?post=${postId}&action=edit`);
    return React.createElement(ToolbarItem, { href: editPostUrl },
        "Edit ",
        postType);
}
