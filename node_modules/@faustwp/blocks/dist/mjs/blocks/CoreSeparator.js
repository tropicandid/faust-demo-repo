import { gql } from '@apollo/client';
import React from 'react';
import { useBlocksTheme } from '../components/WordPressBlocksProvider.js';
import { getStyles } from '../utils/index.js';
export function CoreSeparator(props) {
    const theme = useBlocksTheme();
    const style = getStyles(theme, Object.assign({}, props));
    const { attributes } = props;
    return (React.createElement("hr", { style: style, className: attributes === null || attributes === void 0 ? void 0 : attributes.cssClassName, id: attributes === null || attributes === void 0 ? void 0 : attributes.anchor }));
}
CoreSeparator.fragments = {
    key: `CoreSeparatorBlockFragment`,
    entry: gql `
    fragment CoreSeparatorBlockFragment on CoreSeparator {
      attributes {
        align
        anchor
        opacity
        gradient
        backgroundColor
        style
        cssClassName
      }
    }
  `,
};
CoreSeparator.config = {
    name: 'CoreSeparator',
};
CoreSeparator.displayName = 'CoreSeparator';
